# إصدار القالب 2.0 — ملخص التسليم

هذه النسخة تحوّل المشروع إلى قالب أعمال عربي قابل للنسخ لكل عميل، مع إبقاء بيانات موقع الرياض الحالية كنموذج جاهز.

## أبرز ما أضيف

- إعداد مركزي للعميل عبر `client.config.json`.
- أمر `npm run client:init` لإنشاء ملف إعداد عميل جديد.
- أمر `npm run client:setup` لتطبيق بيانات العميل على الموقع والمحتوى وDecap وSEO.
- نظام صور عبر Sharp يولد WebP وAVIF و`srcset` وأحجامًا متعددة.
- معالجة Incremental: الصور التي لم تتغير لا يعاد ضغطها في كل تشغيل.
- علامة مائية اختيارية مدمجة داخل النسخ المولدة باستخدام شعار الموقع.
- `SmartImage.astro` للتحميل الكسول والأبعاد وأولوية LCP.
- تحسين صور Markdown تلقائيًا.
- لوحة Decap موسعة لإدارة الهوية والصور والعلامة المائية وحقول SEO والتحويلات.
- لوحة `/seo-admin/` موسعة لفحص المحتوى والزحف والفهرسة والأداء والصور.
- `npm run validate` لفحص الإعدادات قبل البناء.
- `npm run audit:dist` لفحص HTML الناتج والروابط الداخلية والصور وSEO بعد البناء.
- `npm run quality` للبناء ثم فحص الناتج.
- GitHub Actions اختياري لفحص كل Push وPull Request.
- IndexNow اختياري.
- ترويسات أمان وكاش مناسبة لـ Cloudflare Pages.

## ملاحظة عن العلامة المائية

العلامة المائية هنا للهوية وحماية العرض العادي، وليست DRM. الأصول الموجودة في `public/uploads/` تظل ملفات عامة إذا عرف الزائر رابطها المباشر. إذا كان المطلوب إخفاء الأصول تمامًا، فذلك يحتاج نظام وسائط خاصًا مثل R2/Images أو تخزينًا غير عام.

## ملاحظة الخطوط

القالب لا يتضمن ملفات خطوط مرفقة. يعمل بخطوط النظام افتراضيًا لتقليل الطلبات وتحسين الأداء. يمكن لصاحب القالب إضافة خط مرخّص لديه بنفسه لاحقًا.
