# تقرير التحقق — Alraqabi Astro Business Template v2.0

## تم التحقق منه في هذه النسخة

- قراءة جميع ملفات JSON الخاصة بالمحتوى والإعدادات بنجاح.
- قراءة `public/admin/config.yml` كـYAML بنجاح.
- فحص Syntax لجميع سكربتات Node `.mjs` الجديدة بنجاح.
- تشغيل `node scripts/validate-template.mjs` على نسخة الرياض الحالية: **0 أخطاء، 0 تحذيرات**.
- تجربة `client:init` و`client:setup` على نسخة منفصلة ببيانات عميل تجريبية ودومين ومدينة وهاتف وRepo/OAuth مختلفين.
- بعد تطبيق العميل التجريبي: **0 أخطاء، 0 تحذيرات**، ولم تبق القيم الحرجة القديمة في الملفات النصية المختبرة.
- التحقق من عدم تضمين ملفات خطوط ثنائية داخل نسخة التسليم.
- إعادة اختبار تحويل القالب إلى عميل تجريبي بعد التعديلات النهائية: **0 أخطاء، 0 تحذيرات**، و**0 بقايا** للقيم الحرجة الخاصة بعميل الرياض في الملفات النصية المختبرة.
- التحقق من أن صور Markdown المولدة داخل `<picture>` تحتفظ بتنسيق مناسب داخل `.prose`.

## ما يحتاج تشغيلًا على جهاز متصل بالإنترنت

بيئة إنشاء الملف هنا لم تتمكن من تنزيل حزم npm من Registry، لذلك لم يكن ممكنًا تنفيذ Astro Build كامل بعد إضافة Sharp. على جهاز التطوير أو Cloudflare Pages نفّذ:

```bash
npm ci
npm run quality
```

`npm run quality` ينفذ Build ثم يفحص HTML الناتج والروابط الداخلية والصور وحقول SEO.

إذا نجح الأمر، فالنسخة جاهزة للنشر.
