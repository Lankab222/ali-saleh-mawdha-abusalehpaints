# نظام الصور الاحترافي

## ماذا يحدث؟

`npm run images:build` يقرأ الصور المدعومة داخل `public/uploads/` ويولد نسخًا خفيفة في:

```text
public/generated/images/
```

كما يولد Manifest في:

```text
src/generated/image-manifest.json
```

`SmartImage.astro` يستخدم الـManifest لتقديم:

- AVIF عندما يدعمه المتصفح.
- WebP كبديل حديث.
- `srcset` وأحجام متعددة.
- `sizes` مناسب لمكان الصورة.
- `loading="lazy"` للصور العادية.
- `fetchpriority="high"` للصور ذات أولوية LCP فقط.
- `width` و`height` للحد من CLS.

## المعالجة Incremental والوسائط غير المستخدمة

السكربت يسجل بصمة للصورة وإعدادات المعالجة. إذا لم تتغير الصورة أو الجودة أو العلامة المائية، يعيد استخدام النسخ السابقة بدل ضغط كل الصور من البداية. كما يحذف الملفات المولدة القديمة التي لم تعد مستخدمة.

افتراضيًا يعالج فقط الصور التي يشير إليها المحتوى أو إعدادات الموقع، ويتخطى الملفات الموجودة في `public/uploads/` وغير المستخدمة فعليًا. هذا يقلل وقت Build بصورة كبيرة عندما يحتوي Media Library على صور قديمة. يمكن تغيير ذلك من خيار **معالجة الصور غير المستخدمة** في إعدادات الموقع.

لوحة `/seo-admin/` تعرض قائمة الوسائط غير المستخدمة لمراجعتها قبل حذفها يدويًا.

## العلامة المائية

من:

```text
/admin/ → إعدادات الموقع → العلامة المائية للصور
```

يمكن ضبط:

- التشغيل والإيقاف.
- شعار العلامة المائية.
- الموقع.
- الشفافية.
- الحجم كنسبة من عرض الصورة.
- الهامش.
- أنواع الصور التي تطبق عليها.

العلامة المائية **تُدمج داخل الملف المولد نفسه**؛ لا تحتاج JavaScript أو طبقة CSS فوق الصورة.

افتراضيًا لا نضع العلامة على Hero أو الشعار، ويمكن وضعها على صور الخدمات والمقالات والمشاريع والمحتوى والبطاقات.

> العلامة المائية للهوية وردع النسخ العادي، وليست DRM. ملفات الأصل داخل `public/uploads/` تبقى عامة إذا عُرف رابطها المباشر. لإخفاء الأصل تمامًا يلزم نظام وسائط خاص مثل Cloudflare R2/Images أو تخزين غير عام.

## صور Markdown

الـRehype plugin يضيف تلقائيًا:

- Lazy Loading.
- Async decoding.
- أبعاد الصورة.
- `srcset` و`sizes` عند وجود الصورة في Manifest.
- النسخة ذات العلامة المائية عندما يكون دور `content` مفعّلًا.

## توصيات الرفع

حتى مع التحسين التلقائي، يفضل:

- Hero: حتى 1600–1920px غالبًا.
- صور المقالات والخدمات: 1200–1600px.
- البطاقات: لا حاجة لصور 4K.
- ارفع أصلًا جيدًا، ودع القالب يولد نسخ العرض.

لوحة `/seo-admin/` تعرض الصور الأصلية التي تتجاوز حد التحذير المحدد في الإعدادات.
